import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["picsum.photos","res.cloudinary.com"], // Agrega el dominio aquí
  },
};

export default nextConfig;
